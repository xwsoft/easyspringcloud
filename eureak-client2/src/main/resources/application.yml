server:
  port: 8082
spring:
  application:
    name: eureka-client-2
eureka:
  client:
    healthcheck:
      enabled: true
    service-url:
      defaultZone: http://admin:admin@localhost:8080/eureka/
  instance:
    prefer-ip-address: true
