server:
  port: 9081
spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/xwsoft/easyspringcloud
          username:
          password:
          search-paths: /config_manage