server:
  port: 9081
spring:
  application:
    name: springcloud-config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/xwsoft/config_manage
          username:
          password: